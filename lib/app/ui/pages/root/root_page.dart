import 'package:flutter/material.dart';
import 'package:warehouse_flow/app/ui/pages/root/root_presenter.dart';

class RootPage extends StatelessWidget {
  final RootPresenter presenter;
  const RootPage(
    this.presenter, {
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
